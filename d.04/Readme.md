# Students Database Branch

This branch contains the SQL schema, sample data, and explanations for a `students` database.  
It demonstrates table creation, primary and unique keys, inserting data, and basic database concepts.

---

## **Database Creation**

Create the database and connect to it:

```sql
CREATE DATABASE db_name;

-- Connect to the database
\c db_name;
