# Backend Express + PostgreSQL Branch

This branch contains a simple **Express.js backend** connected to **PostgreSQL** using the `pg` package.  
It demonstrates database connection, environment variables, and a basic Express server setup.

---

## **Technologies Used**

- Node.js  
- Express.js  
- TypeScript  
- PostgreSQL (`pg` package)  
- dotenv (for environment variables)

---

